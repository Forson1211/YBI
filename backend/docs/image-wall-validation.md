# Image Wall Validation Notes

- Desktop review confirms a dense three-row mosaic with oversized landscape outer rows, a visibly taller 4:5 portrait middle row, and a section that occupies more than one viewport when its heading is included.
- Mobile review confirms the image wall remains contained within the viewport, preserves the three-row arrangement, and keeps the portrait middle row distinct from the landscape outer rows.
- The existing reduced-motion media query continues to disable non-essential animation and transitions for visitors who request reduced motion.
